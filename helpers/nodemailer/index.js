const { emailVerification } = require("./emailVerification");
const { resendEmailVerification } = require("./resendEmailVerification");

module.exports = { emailVerification, resendEmailVerification };
